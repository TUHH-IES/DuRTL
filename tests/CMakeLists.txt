file(GLOB_RECURSE testfiles RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.cpp)
add_executable(tester ${testfiles})
target_link_libraries(tester PRIVATE sources Catch2::Catch2 CLI11::CLI11)
catch_discover_tests(tester)
enable_sanitizers(tester)